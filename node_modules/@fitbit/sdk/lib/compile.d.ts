/// <reference types="node" />
import { ComponentType } from './componentTargets';
import { DiagnosticHandler } from './diagnostics';
import { PolyfillMap } from './plugins/polyfill';
export default function compile({ component, entryPoint, outputDir, defaultLanguage, allowUnknownExternals, onDiagnostic, polyfills, }: {
    component: ComponentType;
    entryPoint: string;
    outputDir?: string;
    defaultLanguage: string;
    allowUnknownExternals?: boolean;
    onDiagnostic?: DiagnosticHandler;
    polyfills?: PolyfillMap;
}): import("stream").Readable;
